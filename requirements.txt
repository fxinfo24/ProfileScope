# Vanta Requirements - Railway Production (Minimal)
# This file is used by Railway for production deployment
# Includes only essential packages for running the web API
# For local development, use requirements-full.txt instead

# Ultra-minimal Railway requirements - just web API essentials
flask>=2.0.0
flask-cors>=3.0.10
Jinja2>=3.1.0
flask-sqlalchemy>=3.1.1
flask-migrate>=3.0.0
psycopg2-binary>=2.9.9

# Web server
gunicorn>=20.1.0

# Task queue 
celery>=5.1.0
redis>=4.0.0

# Environment
python-dotenv>=0.19.0

# Database
SQLAlchemy>=2.0.16
alembic>=1.12.0

# Basic utilities
requests>=2.25.0
PyJWT==2.6.0

# Excluded for faster builds:
# - numpy, pandas (data processing)
# - nltk, transformers (NLP)
# - matplotlib (plotting)
# - All ML/AI packages