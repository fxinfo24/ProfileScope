# Railway-optimized requirements (excludes GUI and heavy ML packages)
# Core dependencies (minimal for Railway)
setuptools>=65.5.1
numpy<2.0.0
pandas>=1.3.0
nltk>=3.6.0
# Removed transformers and torch to avoid spacy dependencies
# matplotlib>=3.7.2

# Web framework
flask>=2.0.0
Jinja2>=3.1.0
flask-sqlalchemy>=3.1.1
flask-login>=0.5.0
flask-migrate>=3.0.0
psycopg2-binary>=2.9.9

# Data handling
requests>=2.25.0
beautifulsoup4>=4.9.0
python-dateutil>=2.8.0
pytz>=2021.1

# Production server
gunicorn>=20.1.0

# Environment variables
python-dotenv>=0.19.0

# Security
Flask-WTF>=1.0.0

# Utilities
pyyaml>=5.4.0
tqdm>=4.60.0
colorlog>=6.6.0

# API integrations
tweepy>=4.0.0
facebook-sdk==3.1.0

# Database
SQLAlchemy>=2.0.16
alembic>=1.12.0

# Task queue
celery>=5.1.0
redis>=4.0.0

# Web interface dependencies
bootstrap-flask>=2.3.0

# Basic text processing (instead of spacy)
textstat>=0.7.3
PyJWT==2.6.0
bcrypt==4.0.1
Pillow>=8.0.0

# Excluded from Railway deployment:
# - PyQt5/PyQt6 (GUI not needed on Railway)
# - spacy + en_core_web_sm (heavy ML model, build issues)
# - mypy/black/flake8/isort (dev tools not needed in production)
# - pytest packages (testing not needed in production)